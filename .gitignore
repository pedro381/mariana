# Clones de referência (contêm .git próprio e quebrariam como submódulo)
# Ignora apenas os clones — a plataforma em /source/ precisa ser versionada e publicada.
/source/references/

# Fotos originais sem tratamento — o site publica as versões de assets/img/
/img/

# Dependências e segredos locais do Worker
node_modules/
.dev.vars
.wrangler/

# Sistema
Thumbs.db
desktop.ini
.DS_Store
